name: Telegram Sync
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
permissions:
  contents: write
  pages: write
  id-token: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci || npm install
        working-directory: bot
      - name: Run bot (poll 30s for 2 minutes)
        run: |
          node bot.js
        working-directory: bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
          RUN_CONTINUOUS: "true"
          POLL_INTERVAL_MS: "15000"
          EXIT_AFTER_MS: "180000"
          NO_WRITE_WEB_MODELS: "false"
      - name: Deploy web without commit (Pages artifact)
        uses: actions/upload-pages-artifact@v3
        with:
          path: web
      - name: Publish to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Commit models.json to repo for consistency
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add web/models.json
          if git diff --cached --quiet; then echo "No changes"; else git commit -m "Sync: update models.json"; git push; fi